---
// const fetchExercises = await fetch("https://wger.de/api/v2/exerciseinfo/"); 

// const responseExercises = await fetchExercises.json();
// const exercises = responseExercises.results;

let exercises = []; 
---

<html lang="en">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="icon" href="/favicon.ico" />
		<meta name="viewport" content="width=device-width" />
		<meta name="generator" content={Astro.generator} />
		<title>Astro</title>

		<link rel="stylesheet" href="/styles/main.css">
	</head>
	<body>
        <header>
            <a href="/">Home</a>
            <div>
                <a href="/workouts">Create Workout</a>
                <a href="/exercises">Exercises</a>
            </div>
        </header>
        <div class="intro">
            <h1>Exercises</h1>
            <p>Browse all available exercises by muscle group.</p>
        </div>
        <div class="search">   
            <input type="text" placeholder="Search exercises...">
        </div>
        <div class="exercises">
            <ul id="exercise-list"></ul>
        </div>

        <script type="module">
            const cached = localStorage.getItem("exercises");

            console.log(cached); 

            if (!cached) {
                document.getElementById("exercise-list").innerHTML =
                    "<p>No data found. Go home first.</p>";
            } else {
                const exercises = JSON.parse(cached);
                const listEl = document.getElementById("exercise-list");

                listEl.innerHTML = exercises
                    .map(
                        (ex) => `
                            <img src=${ex.image} alt="">
                            <a href="/exercise/${ex.id}">
                                <p>${ex.name}</p>
                                <p>${ex.category}</p>
                            </a>
                        `
                    )
                    .join("");
            }
        </script>
	</body>
</html>
