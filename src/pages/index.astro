---
const fetchExercises = await fetch("https://wger.de/api/v2/exerciseinfo/"); 

const responseExercises = await fetchExercises.json();

//Structure exercises data
const exercises = responseExercises.results.map((exercise) => ({
	id: exercise.id,
	name: exercise.translations?.[0]?.name ?? "Unnamed",
	category: exercise.category?.name ?? "Unknown",
	muscles: exercise.muscles?.map(m => m.name ?? m) ?? [],
	equipment: exercise.equipment?.map(e => e.name ?? e) ?? [],
	// image: exercise.images?.[0]?.image ?? null,
	image: exercise.muscles?.map(m => m.image_url_main ?? m) ?? [],
}));

console.log(exercises); 
---

<html lang="en">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="icon" href="/favicon.ico" />
		<meta name="viewport" content="width=device-width" />
		<meta name="generator" content={Astro.generator} />
		<title>FitTrack</title>

		<link rel="stylesheet" href="/styles/main.css">
		<link rel="stylesheet" href="/styles/index.css">
	</head>
	<body>
		<header>
            <a href="/">Home</a>
            <div>
                <a href="/workouts">Create Workout</a>
                <a href="/exercises">Exercises</a>
            </div>
        </header>
		
		<section>
			<div>
				<h1>Your fitness journey</h1>
				<p>Track your workouts, stay consistent, and build routines that actually work for you. Create custom workouts that fit your goals and watch your progress grow with every rep and every session.</p>
				<button>+ Create workout</button>
			</div>
		</section>

		<section>	
			<div>
				<p>Workouts</p>
				<p>2</p>
			</div>
		</section>

		<section>
			<div>
				<h2>Your workouts</h2>
				<a href="/workouts">Create new</a>
			</div>
			<div>

			</div>
		</section>

		<script type="module" define:vars={{ exercises }}>
			localStorage.setItem("exercises", JSON.stringify(exercises));

			console.log(JSON.parse(localStorage.getItem("exercises")));
		</script>
	</body>
</html>
